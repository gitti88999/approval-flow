[pytest]
markers =
    unit: fast, isolated tests of a single module/function with all I/O mocked
    integration: tests of a service's orchestrating handler composing several of its own internal collaborators together, still with the Dapr/HTTP boundary mocked
