apiVersion: dapr.io/v1alpha1
kind: Subscription
metadata:
  name: payment-subscription
spec:
  topic: payment-required
  route: /on-payment-required
  pubsubname: invoice-pubsub
scopes:
- payment-service